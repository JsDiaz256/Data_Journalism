<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 600 500" width="600" height="500">
  <text font-size="20" x="50" y="15" style="text-decoration: underline" >List of Businesses in {{zip}}</text>
  {% for eat in range(zip_eateries|length) %}
    <text font-size="15" x="60" y="{{40+20*eat}}" >{{zip_eateries[eat].split('__')[0]}} -- {{zip_eateries[eat].split('__')[1]}}</text>
  {% endfor %}
</svg>